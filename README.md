# claudectl

Runs an ephemeral, sandboxed Claude Code session in an Apple [`container`](https://github.com/apple/container) Linux VM, mounting only the project directory and injecting Claude credentials from the macOS Keychain.
